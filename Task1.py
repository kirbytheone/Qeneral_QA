import os
import platform
print('Current OS:', platform. uname(), 'Runs with Python version:', platform.python_version());
# print('Current OS:', platform.system(), platform.version(), platform.release(), platform.architecture(), platform.machine(), 'Runs with Python version:', platform.python_version());
